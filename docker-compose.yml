services:
  minihr:
    image: node:20-alpine
    container_name: minihr
    working_dir: /app
    command: sh -c "npm install && node server.js"
    ports:
      - "5050:5050"
    volumes:
      - ./app:/app
      - ./backups:/backups
    restart: always
